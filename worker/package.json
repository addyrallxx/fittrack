{
  "name": "fittrack-push",
  "private": true,
  "type": "module",
  "description": "Reminder server for FitTrack. No dependencies: all crypto is raw Web Crypto.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail",
    "test": "node ../test/schedule.test.mjs"
  }
}
